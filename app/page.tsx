import { getDb } from "@/lib/db";

export const dynamic = "force-dynamic";

type StoreSettings={store_name:string;logo_url:string|null;primary_color:string|null;top_message:string|null;disclosure_text:string|null;price_notice:string|null;instagram_url:string|null;facebook_url:string|null;tiktok_url:string|null;whatsapp_url:string|null;telegram_url:string|null};
type Product={id:number;name:string;description:string|null;regular_price:string|null;promo_price:string|null;destination_url:string|null;affiliate_url:string|null;sale_mode:string|null;product_type:string|null;platform:string|null;button_text:string|null;image_url:string|null;category_name:string|null};
type Category={id:number;name:string;slug:string;product_count:number};

function money(value:string|null){if(!value)return null;const n=Number(value);if(!Number.isFinite(n))return null;return new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(n);}
function platformLabel(value:string|null){const labels:Record<string,string>={mercado_livre:"Mercado Livre",shopee:"Shopee",amazon:"Amazon",hotmart:"Hotmart",proprio:"SHILMASTORE",outra:"Outra plataforma"};return value?labels[value]||value:"";}
function productTypeLabel(value:string|null){return value==="DIGITAL"?"Digital":"Físico";}

export default async function Home(){
  const sql=getDb();
  const [settingsRows,products,categories]=await Promise.all([
    sql`SELECT store_name,logo_url,primary_color,top_message,disclosure_text,price_notice,instagram_url,facebook_url,tiktok_url,whatsapp_url,telegram_url FROM store_settings WHERE id=1 LIMIT 1`,
    sql`SELECT p.id,p.name,p.description,p.regular_price,p.promo_price,p.destination_url,p.affiliate_url,p.sale_mode,p.product_type,p.platform,p.button_text,c.name AS category_name,(SELECT pi.image_url FROM product_images pi WHERE pi.product_id=p.id ORDER BY pi.sort_order ASC,pi.id ASC LIMIT 1) AS image_url FROM products p LEFT JOIN categories c ON c.id=p.category_id WHERE p.active=TRUE ORDER BY p.featured DESC,p.sort_order ASC,p.updated_at DESC`,
    sql`SELECT c.id,c.name,c.slug,COUNT(p.id)::int AS product_count FROM categories c LEFT JOIN products p ON p.category_id=c.id AND p.active=TRUE WHERE c.active=TRUE GROUP BY c.id,c.name,c.slug ORDER BY c.sort_order ASC,c.name ASC`
  ]);
  const settings=settingsRows[0] as StoreSettings|undefined;
  const storeName=settings?.store_name&&settings.store_name!=="Radar de Achados"?settings.store_name:"SHILMASTORE";
  const primaryColor=settings?.primary_color??"#3483FA";
  const topMessage=settings?.top_message||"Ofertas selecionadas para você economizar de verdade!";
  const socialLinks=[["Instagram",settings?.instagram_url],["Facebook",settings?.facebook_url],["TikTok",settings?.tiktok_url],["WhatsApp",settings?.whatsapp_url],["Telegram",settings?.telegram_url]].filter((item):item is [string,string]=>Boolean(item[1]));
  const productList=products as Product[];
  const categoryList=categories as Category[];
  return <main style={{minHeight:"100vh",background:"#f4f7fb",color:"#0b2447"}}>
    <div style={{background:"#0b2447",color:"white",textAlign:"center",padding:"7px 18px",fontSize:12,fontWeight:800}}>{topMessage}</div>
    <header style={{background:"#ffe600",borderBottom:"1px solid #e3cf00"}}><div style={{maxWidth:1240,margin:"0 auto",padding:"13px 24px",display:"flex",alignItems:"center",justifyContent:"space-between",gap:18,flexWrap:"wrap"}}><a href="/" style={{display:"flex",alignItems:"center",gap:12,color:"#0b2447",textDecoration:"none"}}>{settings?.logo_url?<img src={settings.logo_url} alt={storeName} style={{width:48,height:48,objectFit:"contain",borderRadius:10,background:"white",padding:3}}/>:<div style={{width:48,height:48,borderRadius:12,background:primaryColor,color:"white",display:"grid",placeItems:"center",fontWeight:900}}>SS</div>}<div><strong style={{fontSize:23}}>{storeName}</strong><div style={{fontSize:12,fontWeight:600}}>Produtos e ofertas em um só lugar</div></div></a>{socialLinks.length>0?<nav style={{display:"flex",gap:14,flexWrap:"wrap"}}>{socialLinks.map(([label,url])=><a key={label} href={url} target="_blank" rel="noopener noreferrer" style={{color:"#0b2447",fontWeight:800,textDecoration:"none",fontSize:13}}>{label}</a>)}</nav>:null}</div></header>
    {categoryList.length>0?<nav style={{background:"white",borderBottom:"1px solid #e2e8f0"}}><div style={{maxWidth:1240,margin:"0 auto",padding:"10px 24px",display:"flex",gap:9,overflowX:"auto"}}><a href="#ofertas" style={{whiteSpace:"nowrap",padding:"7px 12px",borderRadius:999,background:"#0b2447",color:"white",textDecoration:"none",fontWeight:800,fontSize:12}}>Todos</a>{categoryList.map(c=><a key={c.id} href="#ofertas" style={{whiteSpace:"nowrap",padding:"7px 12px",borderRadius:999,background:"#f1f5f9",color:"#334155",textDecoration:"none",fontWeight:800,fontSize:12,border:"1px solid #e2e8f0"}}>{c.name}{c.product_count>0?` (${c.product_count})`:""}</a>)}</div></nav>:null}
    <section style={{maxWidth:1240,margin:"0 auto",padding:"24px 24px 10px"}}><div style={{background:"#0b2447",borderRadius:18,padding:"28px clamp(24px,5vw,48px)",display:"flex",alignItems:"center",justifyContent:"space-between",gap:24,flexWrap:"wrap"}}><div><span style={{color:"#ffe600",fontWeight:900,fontSize:12,letterSpacing:1}}>DESTAQUES SHILMASTORE</span><h1 style={{color:"white",fontSize:"clamp(30px,4vw,45px)",margin:"8px 0 8px",lineHeight:1.05}}>Ofertas que valem a pena</h1><p style={{color:"#cbd5e1",fontSize:15,lineHeight:1.5,maxWidth:650,margin:0}}>Encontre produtos selecionados e ofertas de parceiros em uma vitrine simples e direta.</p></div><a href="#ofertas" style={{background:"#ffe600",color:"#0b2447",padding:"12px 18px",borderRadius:10,fontWeight:900,textDecoration:"none",whiteSpace:"nowrap"}}>Ver ofertas</a></div></section>
    <section id="ofertas" style={{maxWidth:1240,margin:"0 auto",padding:"24px 24px 48px"}}><div style={{display:"flex",alignItems:"end",justifyContent:"space-between",gap:18,marginBottom:18,flexWrap:"wrap"}}><div><p style={{fontWeight:900,color:primaryColor,letterSpacing:1.4,fontSize:11,margin:"0 0 5px"}}>VITRINE</p><h2 style={{fontSize:"clamp(26px,3vw,34px)",margin:0}}>Ofertas em destaque</h2></div>{productList.length>1?<span style={{color:"#64748b",fontSize:13}}>{productList.length} produtos</span>:null}</div>{productList.length===0?<div style={{background:"white",border:"1px solid #e2e8f0",borderRadius:18,padding:40,textAlign:"center"}}><h2>Nenhum produto disponível</h2><p style={{color:"#64748b"}}>Novas ofertas serão publicadas em breve.</p></div>:<div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(270px,1fr))",gap:20}}>{productList.map(p=>{const current=money(p.promo_price)||money(p.regular_price);const old=p.promo_price?money(p.regular_price):null;const destination=p.destination_url||p.affiliate_url;const isOwn=p.sale_mode==="OWN";const cta=p.button_text||(isOwn?"Comprar agora":"Ver oferta");return <article key={p.id} style={{background:"white",border:"1px solid #e2e8f0",borderRadius:18,overflow:"hidden",boxShadow:"0 6px 20px rgba(15,23,42,.05)",display:"flex",flexDirection:"column",minWidth:0,maxWidth:340}}><div style={{height:220,background:"#fff",display:"grid",placeItems:"center",padding:18,borderBottom:"1px solid #f1f5f9"}}>{p.image_url?<img src={p.image_url} alt={p.name} style={{maxWidth:"90%",maxHeight:190,width:"auto",height:"auto",objectFit:"contain"}}/>:<div style={{color:"#94a3b8"}}>Sem imagem</div>}</div><div style={{padding:18,display:"flex",flexDirection:"column",gap:10,flex:1}}><div style={{display:"flex",gap:6,flexWrap:"wrap"}}>{p.category_name?<span style={{fontSize:10,fontWeight:900,color:primaryColor,textTransform:"uppercase"}}>{p.category_name}</span>:null}<span style={{fontSize:10,fontWeight:800,color:"#475569",background:"#f1f5f9",padding:"3px 7px",borderRadius:999}}>{isOwn?"Produto próprio":"Afiliado"}</span><span style={{fontSize:10,fontWeight:800,color:"#475569",background:"#f1f5f9",padding:"3px 7px",borderRadius:999}}>{productTypeLabel(p.product_type)}</span>{platformLabel(p.platform)?<span style={{fontSize:10,fontWeight:800,color:"#475569",background:"#f1f5f9",padding:"3px 7px",borderRadius:999}}>{platformLabel(p.platform)}</span>:null}</div><h3 style={{fontSize:18,lineHeight:1.3,margin:0}}>{p.name}</h3>{p.description?<p style={{color:"#64748b",fontSize:13,lineHeight:1.45,margin:0}}>{p.description}</p>:null}<div style={{marginTop:"auto",paddingTop:6}}>{old?<div style={{color:"#94a3b8",fontSize:12,textDecoration:"line-through"}}>{old}</div>:null}<strong style={{fontSize:24,color:"#111827"}}>{current||"Consulte a oferta"}</strong></div>{destination?<a href={`/go/${p.id}?source=site`} target="_blank" rel={isOwn?"noopener noreferrer":"noopener noreferrer sponsored"} style={{display:"block",textAlign:"center",background:primaryColor,color:"white",padding:"12px 15px",borderRadius:10,fontWeight:900,textDecoration:"none"}}>{cta}</a>:<span style={{display:"block",textAlign:"center",background:"#e2e8f0",color:"#64748b",padding:"12px 15px",borderRadius:10,fontWeight:800}}>Link indisponível</span>}</div></article>})}</div>}</section>
    <footer style={{borderTop:"1px solid #dbe3ef",background:"white"}}><div style={{maxWidth:1240,margin:"0 auto",padding:"26px 24px",display:"grid",gridTemplateColumns:"minmax(190px,.7fr) minmax(280px,1.5fr)",gap:28,color:"#64748b",fontSize:12,lineHeight:1.6}}><div><strong style={{display:"block",fontSize:17,color:"#0b2447",marginBottom:5}}>{storeName}</strong><span>Produtos e ofertas em um só lugar.</span></div><div>{settings?.disclosure_text&&!settings.disclosure_text.includes("Radar de Achados")?<p style={{margin:"0 0 6px"}}>{settings.disclosure_text}</p>:<p style={{margin:"0 0 6px"}}>Alguns links podem ser de afiliados. A SHILMASTORE pode receber comissão por compras qualificadas, sem custo adicional para você.</p>}{settings?.price_notice?<p style={{margin:0}}>{settings.price_notice}</p>:<p style={{margin:0}}>Preços e disponibilidade podem mudar sem aviso prévio. Confira as condições na página de compra.</p>}</div></div></footer>
  </main>;
}
