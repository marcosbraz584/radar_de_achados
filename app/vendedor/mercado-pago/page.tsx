import Link from "next/link";import {redirect} from "next/navigation";import {getCurrentUser} from "@/lib/auth";import {getDb} from "@/lib/db";
export const dynamic="force-dynamic";
export default async function Page(){const user=await getCurrentUser();if(!user)redirect("/entrar");const sql=getDb();const s=await sql`SELECT s.id,st.store_name,mp.mercado_pago_user_id,mp.connected_at FROM sellers s LEFT JOIN seller_stores st ON st.seller_id=s.id LEFT JOIN seller_mercado_pago_accounts mp ON mp.seller_id=s.id WHERE s.user_id=${Number(user.id)} AND s.approval_status='approved' LIMIT 1`;const seller=s[0] as any;if(!seller)redirect("/minha-conta");const connected=!!seller.mercado_pago_user_id;return <main style={{minHeight:"100vh",background:"#f5f7fb",padding:"32px 16px",color:"#172554"}}><section style={{maxWidth:760,margin:"auto"}}><Link href="/vendedor">← Painel do vendedor</Link><h1>Recebimentos Mercado Pago</h1><div style={{background:"white",border:"1px solid #e2e8f0",borderRadius:14,padding:22}}><h2>{seller.store_name||"Minha loja"}</h2><p>{connected?"Conta Mercado Pago vinculada à SHILMASTORE.":"Para receber pelas vendas, vincule sua conta Mercado Pago à SHILMASTORE."}</p>{connected?<strong style={{color:"#15803d"}}>✓ Conta vinculada</strong>:<a href="/api/mercado-pago/oauth/iniciar" style={{display:"inline-block",background:"#009ee3",color:"white",padding:"12px 18px",borderRadius:9,textDecoration:"none",fontWeight:900}}>Vincular Mercado Pago</a>}</div></section></main>}