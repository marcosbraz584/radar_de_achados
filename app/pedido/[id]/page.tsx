import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/auth";
import { getDb } from "@/lib/db";

export const dynamic = "force-dynamic";
const SITE_URL = "https://shilmastore.com";
function money(v:number|string){return new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(Number(v)||0)}
function safeMpError(status:number,data:any){const message=String(data?.message||data?.error||"Resposta sem mensagem").slice(0,300);const cause=Array.isArray(data?.cause)?data.cause.map((c:any)=>String(c?.description||c?.code||"")).filter(Boolean).join(" | ").slice(0,400):"";return `HTTP ${status}: ${message}${cause?` — ${cause}`:""}`}

async function startMercadoPagoPayment(formData:FormData){
 "use server";const user=await getCurrentUser();if(!user)redirect("/entrar");const orderId=Number(formData.get("order_id"));if(!Number.isInteger(orderId))redirect("/minha-conta");const token=process.env.MERCADO_PAGO_ACCESS_TOKEN;if(!token)redirect(`/pedido/${orderId}?payment_error=config&payment_detail=${encodeURIComponent("Credencial MERCADO_PAGO_ACCESS_TOKEN não encontrada no servidor")}`);const sql=getDb();const orderRows=await sql`SELECT id,customer_email,total,total_amount,payment_status FROM orders WHERE id=${orderId} AND user_id=${Number(user.id)} LIMIT 1`;const order=orderRows[0] as any;if(!order)redirect("/minha-conta");if(order.payment_status==="approved")redirect(`/pedido/${orderId}`);const itemRows=await sql`SELECT product_name,quantity,unit_price FROM order_items WHERE order_id=${orderId} ORDER BY id`;if(!itemRows.length)redirect(`/pedido/${orderId}?payment_error=items`);const items=itemRows.map((item:any)=>({title:String(item.product_name).slice(0,120),quantity:Number(item.quantity),currency_id:"BRL",unit_price:Number(item.unit_price)}));let response:Response;let data:any={};try{response=await fetch("https://api.mercadopago.com/checkout/preferences",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({items,payer:{email:order.customer_email||user.email},external_reference:`shilmastore_${orderId}`,back_urls:{success:`${SITE_URL}/pedido/${orderId}?payment_return=success`,failure:`${SITE_URL}/pedido/${orderId}?payment_return=failure`,pending:`${SITE_URL}/pedido/${orderId}?payment_return=pending`},auto_return:"approved",notification_url:`${SITE_URL}/api/mercado-pago/webhook`,statement_descriptor:"SHILMASTORE"}),cache:"no-store"});data=await response.json().catch(()=>({}));}catch(error:any){redirect(`/pedido/${orderId}?payment_error=network&payment_detail=${encodeURIComponent(`Falha de conexão: ${String(error?.message||error).slice(0,300)}`)}`)}const checkoutUrl=data?.sandbox_init_point||data?.init_point;if(!response!.ok||!data?.id||!checkoutUrl){const detail=safeMpError(response!.status,data);console.error("Mercado Pago preference error",detail);redirect(`/pedido/${orderId}?payment_error=mercado_pago&payment_detail=${encodeURIComponent(detail)}`)}await sql`UPDATE orders SET payment_provider='mercado_pago',payment_reference=${String(data.id)},updated_at=NOW() WHERE id=${orderId} AND user_id=${Number(user.id)}`;redirect(String(checkoutUrl));
}

async function approvePayment(orderId:number,expectedTotal:number,payment:any){if(String(payment.external_reference)!==`shilmastore_${orderId}`||String(payment.currency_id)!=="BRL"||Math.abs(Number(payment.transaction_amount)-expectedTotal)>0.01||String(payment.status)!=="approved")return false;const sql=getDb();await sql`UPDATE orders SET payment_status='approved',status='paid',payment_provider='mercado_pago',payment_reference=${String(payment.id)},paid_at=COALESCE(paid_at,NOW()),updated_at=NOW() WHERE id=${orderId}`;return true;}
async function reconcilePayment(orderId:number,expectedTotal:number,paymentId?:string){const token=process.env.MERCADO_PAGO_ACCESS_TOKEN;if(!token)return false;try{if(paymentId&&/^\d+$/.test(paymentId)){const r=await fetch(`https://api.mercadopago.com/v1/payments/${paymentId}`,{headers:{Authorization:`Bearer ${token}`},cache:"no-store"});if(r.ok&&await approvePayment(orderId,expectedTotal,await r.json()))return true;}const search=await fetch(`https://api.mercadopago.com/v1/payments/search?external_reference=${encodeURIComponent(`shilmastore_${orderId}`)}&sort=date_created&criteria=desc`,{headers:{Authorization:`Bearer ${token}`},cache:"no-store"});if(!search.ok)return false;const data=await search.json();for(const payment of Array.isArray(data?.results)?data.results:[]){if(await approvePayment(orderId,expectedTotal,payment))return true;}return false;}catch(error){console.error("Mercado Pago reconciliation error",error);return false;}}

export default async function OrderPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{payment_error?:string;payment_return?:string;payment_detail?:string;payment_id?:string;collection_id?:string}>}){const user=await getCurrentUser();if(!user)redirect("/entrar");const {id}=await params;const query=await searchParams;const orderId=Number(id);if(!Number.isInteger(orderId))notFound();const sql=getDb();let rows=await sql`SELECT id,status,payment_status,total,total_amount,payment_provider,payment_reference,created_at FROM orders WHERE id=${orderId} AND user_id=${Number(user.id)} LIMIT 1`;let order=rows[0] as any;if(!order)notFound();const total=Number(order.total_amount??order.total??0);if(order.payment_status!=="approved"&&order.payment_provider==="mercado_pago"){const confirmed=await reconcilePayment(orderId,total,query.payment_id||query.collection_id);if(confirmed){rows=await sql`SELECT id,status,payment_status,total,total_amount,payment_provider,payment_reference,created_at FROM orders WHERE id=${orderId} AND user_id=${Number(user.id)} LIMIT 1`;order=rows[0] as any;}}const items=await sql`SELECT product_name,product_type,quantity,unit_price,subtotal FROM order_items WHERE order_id=${orderId} ORDER BY id`;const paid=order.payment_status==="approved";return <main style={{minHeight:"100vh",background:"#f5f7fb",padding:"30px 16px",color:"#172554"}}><section style={{maxWidth:850,margin:"auto"}}><Link href="/" style={{color:"#2563eb",fontWeight:800,textDecoration:"none"}}>← Voltar à vitrine</Link><h1>Pedido #{order.id}</h1>{query.payment_error&&<div style={{...card,marginBottom:14,background:"#fff7ed",borderColor:"#fdba74"}}><strong>Diagnóstico do pagamento</strong><div>Não foi possível iniciar o Mercado Pago.</div>{query.payment_detail&&<div style={{marginTop:10}}>{query.payment_detail}</div>}</div>}{paid&&<div style={{...card,marginBottom:14,background:"#ecfdf5",borderColor:"#86efac"}}><strong>Pagamento confirmado pelo Mercado Pago.</strong></div>}<div style={card}><div style={{display:"flex",justifyContent:"space-between",gap:15,flexWrap:"wrap"}}><div><b>Status do pedido</b><div>{paid?"Pagamento confirmado":"Pendente de pagamento"}</div></div><div><b>Pagamento</b><div>{paid?"Aprovado":"Pendente"}</div></div><div><b>Total</b><div style={{fontSize:24,fontWeight:900}}>{money(total)}</div></div></div></div><div style={{...card,marginTop:14}}><h2 style={{marginTop:0}}>Itens do pedido</h2>{items.map((item:any,index:number)=><div key={index} style={{padding:"12px 0",borderTop:"1px solid #e2e8f0",display:"flex",justifyContent:"space-between",gap:12}}><div><strong>{item.product_name}</strong><div style={{fontSize:13,color:"#64748b"}}>{item.product_type==="DIGITAL"?"Produto digital":"Produto físico"} • Qtd. {item.quantity}</div></div><strong>{money(item.subtotal)}</strong></div>)}</div>{!paid&&<div style={{...card,marginTop:14,background:"#eff6ff"}}><strong>Próxima etapa: pagamento</strong><p style={{color:"#475569"}}>Você será direcionado ao ambiente seguro do Mercado Pago. Neste momento estamos usando as credenciais de teste.</p><form action={startMercadoPagoPayment}><input type="hidden" name="order_id" value={order.id}/><button type="submit" style={{border:0,borderRadius:10,padding:"13px 20px",background:"#009ee3",color:"white",fontWeight:900,cursor:"pointer",fontSize:15}}>Pagar com Mercado Pago</button></form></div>}</section></main>;}const card={background:"white",border:"1px solid #e2e8f0",borderRadius:14,padding:20,boxShadow:"0 5px 20px #00000008"};
