import Link from "next/link";
import { notFound,redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/auth";
import { getDb } from "@/lib/db";
export const dynamic="force-dynamic";
function money(v:number|string){return new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(Number(v)||0)}
export default async function OrderPage({params}:{params:Promise<{id:string}>}){const user=await getCurrentUser();if(!user)redirect("/entrar");const {id}=await params;const orderId=Number(id);if(!Number.isInteger(orderId))notFound();const sql=getDb();const rows=await sql`SELECT id,status,payment_status,total,created_at FROM orders WHERE id=${orderId} AND user_id=${Number(user.id)} LIMIT 1`;const order=rows[0] as any;if(!order)notFound();const items=await sql`SELECT product_name,product_type,quantity,unit_price,subtotal FROM order_items WHERE order_id=${orderId} ORDER BY id`;return <main style={{minHeight:"100vh",background:"#f5f7fb",padding:"30px 16px",color:"#172554"}}><section style={{maxWidth:850,margin:"auto"}}><Link href="/" style={{color:"#2563eb",fontWeight:800,textDecoration:"none"}}>← Voltar à vitrine</Link><h1>Pedido #{order.id}</h1><div style={card}><div style={{display:"flex",justifyContent:"space-between",gap:15,flexWrap:"wrap"}}><div><b>Status do pedido</b><div>Pendente de pagamento</div></div><div><b>Pagamento</b><div>Pendente</div></div><div><b>Total</b><div style={{fontSize:24,fontWeight:900}}>{money(order.total)}</div></div></div></div><div style={{...card,marginTop:14}}><h2 style={{marginTop:0}}>Itens do pedido</h2>{items.map((item:any,index:number)=><div key={index} style={{padding:"12px 0",borderTop:"1px solid #e2e8f0",display:"flex",justifyContent:"space-between",gap:12}}><div><strong>{item.product_name}</strong><div style={{fontSize:13,color:"#64748b"}}>{item.product_type==="DIGITAL"?"Produto digital":"Produto físico"} • Qtd. {item.quantity}</div></div><strong>{money(item.subtotal)}</strong></div>)}</div><div style={{...card,marginTop:14,background:"#eff6ff"}}><strong>Próxima etapa: pagamento</strong><p style={{marginBottom:0,color:"#475569"}}>O pedido já está salvo. A integração com o meio de pagamento será conectada antes de liberar produtos digitais ou confirmar pedidos físicos.</p></div></section></main>}
const card={background:"white",border:"1px solid #e2e8f0",borderRadius:14,padding:20,boxShadow:"0 5px 20px #00000008"};
