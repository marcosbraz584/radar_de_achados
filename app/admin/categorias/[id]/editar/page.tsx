import { getDb } from "@/lib/db";
import { notFound,redirect } from "next/navigation";
export const dynamic="force-dynamic";
function makeSlug(value:string){return value.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"");}
async function getCategory(id:number){const sql=getDb();const rows=await sql`SELECT id,name,slug,parent_id,active FROM categories WHERE id=${id} LIMIT 1`;return rows[0]||null;}
async function getParents(id:number){const sql=getDb();return sql`SELECT id,name FROM categories WHERE parent_id IS NULL AND id<>${id} ORDER BY name`;}
async function updateCategory(formData:FormData){"use server";const id=Number(formData.get("id"));const name=String(formData.get("name")||"").trim();const slugInput=String(formData.get("slug")||"").trim();const parentRaw=String(formData.get("parent_id")||"").trim();const parentId=parentRaw?Number(parentRaw):null;const active=formData.get("active")==="on";if(!Number.isInteger(id)||id<=0||!name)throw new Error("Categoria inválida.");if(parentId===id)throw new Error("Uma categoria não pode ser sua própria categoria principal.");const slug=makeSlug(slugInput||name);const sql=getDb();await sql`UPDATE categories SET name=${name},slug=${slug},parent_id=${parentId},active=${active},updated_at=NOW() WHERE id=${id}`;redirect("/admin/categorias");}
export default async function EditarCategoriaPage({params}:{params:Promise<{id:string}>}){const {id:raw}=await params;const id=Number(raw);if(!Number.isInteger(id)||id<=0)notFound();const category:any=await getCategory(id);if(!category)notFound();const parents:any[]=await getParents(id) as any[];return <main className="admin-shell"><aside className="admin-sidebar"><div className="admin-brand"><span className="admin-logo">RA</span><div><strong>Radar de Achados</strong><small>Painel 2.0</small></div></div><nav className="admin-nav"><a href="/admin">Visão geral</a><a href="/admin/produtos">Produtos</a><a className="active" href="/admin/categorias">Categorias</a><span>Cupons</span><span>Banners</span><span>Cliques</span><span>Configurações</span></nav></aside><section className="admin-content"><header className="admin-header"><div><p className="eyebrow">ORGANIZAÇÃO</p><h1>Editar categoria</h1><p className="admin-subtitle">Altere o nome, endereço e organização da categoria.</p></div><a className="secondary-button" href="/admin/categorias">← Voltar</a></header><form action={updateCategory} className="product-form"><input type="hidden" name="id" value={category.id}/><section className="form-card"><div className="form-grid"><label className="field field-full"><span>Nome *</span><input name="name" required defaultValue={category.name}/></label><label className="field field-full"><span>Slug</span><input name="slug" defaultValue={category.slug||""}/></label><label className="field field-full"><span>Categoria principal</span><select name="parent_id" defaultValue={category.parent_id?String(category.parent_id):""}><option value="">Nenhuma — categoria principal</option>{parents.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><div className="check-fields field-full"><label><input type="checkbox" name="active" defaultChecked={Boolean(category.active)}/> Categoria ativa</label></div></div></section><div className="form-actions"><a className="secondary-button" href="/admin/categorias">Cancelar</a><button className="primary-button" type="submit">Salvar alterações</button></div></form></section></main>;}
