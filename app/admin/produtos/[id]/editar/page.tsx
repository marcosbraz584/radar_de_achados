import { getDb } from "@/lib/db";
import { notFound, redirect } from "next/navigation";
import ExcluirProdutoButton from "./ExcluirProdutoButton";
export const dynamic="force-dynamic";
function parseBrazilianPrice(value:FormDataEntryValue|null){const raw=String(value||"").trim();if(!raw)return null;const cleaned=raw.replace(/\s/g,"").replace(/^R\$/i,"");const normalized=cleaned.includes(",")?cleaned.replace(/\./g,"").replace(",", "."):cleaned;const numeric=Number(normalized);return Number.isFinite(numeric)&&numeric>=0?numeric:null;}
function inputPrice(value:unknown){if(value==null||value==="")return "";const numeric=Number(value);return Number.isFinite(numeric)?numeric.toFixed(2).replace(".",","):"";}
function formatPrice(value:unknown){if(value==null||value==="")return "—";const numeric=Number(value);return Number.isFinite(numeric)?new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(numeric):"—";}
function formatHistoryDate(value:unknown){const date=new Date(String(value));if(Number.isNaN(date.getTime()))return "—";return new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short",timeZone:"America/Fortaleza"}).format(date);}
function samePrice(a:unknown,b:number|null){if(a==null||a==="")return b===null;return b!==null&&Number(a)===b;}
function isValidHttpUrl(value:string){try{const url=new URL(value);return url.protocol==="https:"||url.protocol==="http:";}catch{return false;}}
async function getProduct(id:number){const sql=getDb();const rows=await sql`SELECT id,name,slug,description,product_type,sale_mode,platform,category_id,regular_price,promo_price,affiliate_url,destination_url,payment_provider,marketplace_url,marketplace_item_id,active,featured,price_source FROM products WHERE id=${id} LIMIT 1`;return rows[0]||null;}
async function getCategories(){const sql=getDb();return sql`SELECT c.id,c.name,p.name AS parent_name FROM categories c LEFT JOIN categories p ON p.id=c.parent_id WHERE c.active=TRUE ORDER BY COALESCE(p.name,c.name),c.parent_id NULLS FIRST,c.name`;}
async function getPriceHistory(id:number){const sql=getDb();return sql`SELECT id,regular_price,promo_price,captured_at,source FROM price_history WHERE product_id=${id} ORDER BY captured_at DESC LIMIT 20`;}
async function updateProduct(formData:FormData){"use server";const id=Number(formData.get("id"));if(!Number.isInteger(id)||id<=0)throw new Error("Produto inválido.");const name=String(formData.get("name")||"").trim(),slugInput=String(formData.get("slug")||"").trim(),description=String(formData.get("description")||"").trim(),destinationUrl=String(formData.get("destination_url")||"").trim(),marketplaceUrl=String(formData.get("marketplace_url")||"").trim(),marketplaceItemId=String(formData.get("marketplace_item_id")||"").trim();const productType=String(formData.get("product_type")||"FISICO")==="DIGITAL"?"DIGITAL":"FISICO";const saleMode=String(formData.get("sale_mode")||"AFFILIATE")==="OWN"?"OWN":"AFFILIATE";const platform=String(formData.get("platform")||"").trim();const paymentProvider=saleMode==="OWN"?String(formData.get("payment_provider")||"mercado_pago").trim():null;const affiliateUrl=saleMode==="AFFILIATE"?destinationUrl:null;const categoryRaw=String(formData.get("category_id")||"").trim();const categoryId=categoryRaw?Number(categoryRaw):null;const regularPrice=parseBrazilianPrice(formData.get("regular_price")),promoPrice=parseBrazilianPrice(formData.get("promo_price")),active=formData.get("active")==="on",featured=formData.get("featured")==="on";if(!name)throw new Error("Nome do produto é obrigatório.");if(!destinationUrl||!isValidHttpUrl(destinationUrl))throw new Error(saleMode==="AFFILIATE"?"Informe um link de afiliado válido (http ou https) antes de salvar/publicar o produto.":"Informe um link de destino válido (http ou https).");if(active&&saleMode==="AFFILIATE"&&!affiliateUrl)throw new Error("Produto afiliado não pode ser publicado sem link de afiliado.");const slug=(slugInput||name).normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"");const sql=getDb();const rows=await sql`SELECT regular_price,promo_price,price_source FROM products WHERE id=${id} LIMIT 1`;const current:any=rows[0];if(!current)throw new Error("Produto não encontrado.");const priceChanged=!samePrice(current.regular_price,regularPrice)||!samePrice(current.promo_price,promoPrice);if(priceChanged)await sql`INSERT INTO price_history(product_id,regular_price,promo_price,source) VALUES (${id},${current.regular_price??null},${current.promo_price??null},'manual')`;const nextPriceSource=priceChanged?"manual":String(current.price_source||"manual");await sql`UPDATE products SET name=${name},slug=${slug},description=${description||null},product_type=${productType},sale_mode=${saleMode},platform=${platform||null},category_id=${categoryId},regular_price=${regularPrice},promo_price=${promoPrice},affiliate_url=${affiliateUrl},destination_url=${destinationUrl},payment_provider=${paymentProvider},marketplace_url=${marketplaceUrl||null},marketplace_item_id=${marketplaceItemId||null},price_source=${nextPriceSource},updated_at=NOW(),active=${active},featured=${featured} WHERE id=${id}`;redirect("/admin/produtos");}
async function deleteProduct(formData:FormData){"use server";const id=Number(formData.get("id"));if(!Number.isInteger(id)||id<=0)throw new Error("Produto inválido.");const sql=getDb();await sql`DELETE FROM products WHERE id=${id}`;redirect("/admin/produtos");}
export default async function EditarProdutoPage({params}:{params:Promise<{id:string}>}){const {id:rawId}=await params;const id=Number(rawId);if(!Number.isInteger(id)||id<=0)notFound();const product:any=await getProduct(id);if(!product)notFound();const [history,categories]=await Promise.all([getPriceHistory(id),getCategories()]) as [any[],any[]];const destination=product.destination_url||product.affiliate_url||"";return <main className="admin-shell"><aside className="admin-sidebar"><div className="admin-brand"><span className="admin-logo">RA</span><div><strong>Radar de Achados</strong><small>Painel 2.0</small></div></div><nav className="admin-nav"><a href="/admin">Visão geral</a><a className="active" href="/admin/produtos">Produtos</a><a href="/admin/categorias">Categorias</a><a href="/admin/cupons">Cupons</a><a href="/admin/banners">Banners</a><a href="/admin/cliques">Cliques</a><a href="/admin/configuracoes">Configurações</a></nav></aside><section className="admin-content"><header className="admin-header"><div><p className="eyebrow">CATÁLOGO MULTICANAL</p><h1>Editar produto</h1><p className="admin-subtitle">Gerencie produtos próprios e afiliados no mesmo painel.</p></div><a className="secondary-button" href="/admin/produtos">← Voltar</a></header><form action={updateProduct} className="product-form"><input type="hidden" name="id" value={product.id}/><section className="form-card"><h2>Tipo de produto</h2><div className="form-grid two-columns"><label className="field"><span>Origem da venda</span><select name="sale_mode" defaultValue={product.sale_mode||"AFFILIATE"}><option value="AFFILIATE">Produto afiliado / de terceiros</option><option value="OWN">Produto próprio</option></select></label><label className="field"><span>Formato</span><select name="product_type" defaultValue={product.product_type||"FISICO"}><option value="FISICO">Produto físico</option><option value="DIGITAL">Produto digital</option></select></label><label className="field field-full"><span>Plataforma / origem</span><select name="platform" defaultValue={product.platform||"mercado_livre"}><option value="mercado_livre">Mercado Livre</option><option value="shopee">Shopee</option><option value="amazon">Amazon</option><option value="proprio">Produto próprio</option><option value="hotmart">Hotmart</option><option value="outra">Outra plataforma</option></select></label></div></section><section className="form-card"><h2>Informações principais</h2><div className="form-grid"><label className="field field-full"><span>Nome do produto *</span><input name="name" required defaultValue={product.name}/></label><label className="field field-full"><span>Categoria</span><select name="category_id" defaultValue={product.category_id?String(product.category_id):""}><option value="">Sem categoria</option>{categories.map(c=><option key={c.id} value={c.id}>{c.parent_name?`${c.parent_name} › ${c.name}`:c.name}</option>)}</select></label><label className="field field-full"><span>Slug</span><input name="slug" defaultValue={product.slug||""}/></label><label className="field field-full"><span>Descrição</span><textarea name="description" rows={5} defaultValue={product.description||""}/></label></div></section><section className="form-card"><h2>Preço e oferta</h2><div className="form-grid two-columns"><label className="field"><span>Preço normal</span><input name="regular_price" inputMode="decimal" defaultValue={inputPrice(product.regular_price)}/></label><label className="field"><span>Preço promocional</span><input name="promo_price" inputMode="decimal" defaultValue={inputPrice(product.promo_price)}/></label></div></section><section className="form-card"><h2>Histórico de preços</h2><p className="admin-subtitle">Últimas alterações registradas para este produto.</p>{history.length===0?<p style={{marginTop:16,color:"#64748b"}}>Nenhuma alteração de preço registrada ainda.</p>:<div style={{marginTop:16,display:"grid",gap:10}}>{history.map((entry:any)=><div key={entry.id} style={{display:"flex",justifyContent:"space-between",gap:20,alignItems:"center",padding:"12px 14px",border:"1px solid #e2e8f0",borderRadius:12,background:"#f8fafc"}}><div><strong>{formatPrice(entry.promo_price)}</strong>{entry.regular_price!==null&&<span style={{marginLeft:10,color:"#64748b"}}>Normal: {formatPrice(entry.regular_price)}</span>}<span style={{marginLeft:10,padding:"3px 8px",borderRadius:999,background:entry.source==="automatic"?"#dbeafe":"#fef3c7",color:entry.source==="automatic"?"#1d4ed8":"#92400e",fontSize:12,fontWeight:700}}>{entry.source==="automatic"?"Automático":"Manual"}</span></div><small style={{color:"#64748b"}}>{formatHistoryDate(entry.captured_at)}</small></div>)}</div>}</section><section className="form-card"><h2>Destino da compra</h2><div className="form-grid"><label className="field field-full"><span>Link para comprar / link de afiliado *</span><input type="url" name="destination_url" required defaultValue={destination}/></label><label className="field"><span>Pagamento para produto próprio</span><select name="payment_provider" defaultValue={product.payment_provider||"mercado_pago"}><option value="mercado_pago">Mercado Pago</option><option value="outro">Outro checkout</option></select></label></div></section><section className="form-card"><h2>Mercado Livre — automação opcional</h2><div className="form-grid"><label className="field field-full"><span>Link original do produto</span><input type="url" name="marketplace_url" defaultValue={product.marketplace_url||""}/></label><label className="field field-full"><span>Código MLB do anúncio</span><input name="marketplace_item_id" defaultValue={product.marketplace_item_id||""}/></label></div></section><section className="form-card"><h2>Publicação</h2><div className="check-fields"><label><input type="checkbox" name="active" defaultChecked={Boolean(product.active)}/> Produto ativo</label><label><input type="checkbox" name="featured" defaultChecked={Boolean(product.featured)}/> Produto em destaque</label></div><p style={{margin:"10px 0 0",fontSize:"12px",color:"#64748b"}}>Produtos afiliados só podem ser publicados com um link de afiliado válido.</p></section><div className="form-actions"><ExcluirProdutoButton action={deleteProduct}/><a className="secondary-button" href="/admin/produtos">Cancelar</a><button className="primary-button" type="submit">Salvar alterações</button></div></form></section></main>;}
