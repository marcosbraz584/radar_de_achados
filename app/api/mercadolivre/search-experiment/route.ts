import { NextResponse } from "next/server";
import { getMercadoLivreAccessToken } from "@/lib/mercadolivre";

export const dynamic = "force-dynamic";

type DomainSuggestion = { domain_id?: string; domain_name?: string; category_id?: string; category_name?: string };
type MarketplaceItem = { id?: string; title?: string; price?: number; original_price?: number|null; currency_id?: string; category_id?: string; catalog_product_id?: string|null; permalink?: string; thumbnail?: string };

async function mlFetch(url:string){let token=await getMercadoLivreAccessToken();let response=await fetch(url,{headers:{Authorization:`Bearer ${token}`},cache:"no-store"});if(response.status===401){token=await getMercadoLivreAccessToken(true);response=await fetch(url,{headers:{Authorization:`Bearer ${token}`},cache:"no-store"})}return response}
async function predict(q:string){const params=new URLSearchParams({q,limit:"3"});const response=await mlFetch(`https://api.mercadolibre.com/sites/MLB/domain_discovery/search?${params}`);const data=await response.json();return(Array.isArray(data)?data:[]) as DomainSuggestion[]}
async function searchMarketplace(q:string,categoryId:string){const params=new URLSearchParams({q,category:categoryId,limit:"50"});const response=await mlFetch(`https://api.mercadolibre.com/sites/MLB/search?${params}`);const data=await response.json();const items:MarketplaceItem[]=Array.isArray(data?.results)?data.results:[];const unique=new Map<string,MarketplaceItem>();for(const item of items){const key=item.catalog_product_id||item.id;if(key&&!unique.has(key))unique.set(key,item)}const rows=[...unique.values()].filter(item=>item.id&&typeof item.price==="number").slice(0,24).map((item,index)=>({id:item.catalog_product_id||item.id||`item-${index}`,name:item.title||null,status:"active",image:item.thumbnail||null,pictures:item.thumbnail?[item.thumbnail]:[],features:[],permalink:item.permalink||null,offer_item_id:item.id||null,offer_price:typeof item.price==="number"?item.price:null,offer_original_price:typeof item.original_price==="number"?item.original_price:null,currency_id:item.currency_id||"BRL",offers_count:1}));return{ok:response.ok,status:response.status,total:data?.paging?.total??items.length,error:response.ok?null:data?.message||data?.error||"Erro na consulta",results:rows}}

export async function GET(request:Request){try{const q=new URL(request.url).searchParams.get("q")?.trim()||"";if(q.length<2)return NextResponse.json({ok:false,error:"Digite pelo menos 2 caracteres."},{status:400});const suggestions=await predict(q);const predicted=suggestions[0]??null;const categoryId=predicted?.category_id;if(!categoryId)return NextResponse.json({ok:false,error:"Categoria não prevista pelo Mercado Livre."});const marketplace=await searchMarketplace(q,categoryId);return NextResponse.json({ok:marketplace.ok,query:q,total:marketplace.total,results:marketplace.results,prediction:{domain_id:predicted?.domain_id??null,domain_name:predicted?.domain_name??null,category_id:categoryId,category_name:predicted?.category_name??null},error:marketplace.error})}catch(error){console.error("Erro no experimento de busca Mercado Livre",error);return NextResponse.json({ok:false,error:error instanceof Error?error.message:"Erro interno."},{status:500})}}
